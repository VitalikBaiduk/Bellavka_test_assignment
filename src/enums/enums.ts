export enum PriceType {
  CURRENT = "CURRENT",
  OLD = "OLD",
  SECOND_OPTION_OF_PRICE = "SECOND_OPTION_OF_PRICE",
}

export enum SizeType {
  SIZE = "SIZE",
  HEIGTH = "HEIGTH",
}

export enum ModalType {
  SIZES = "SIZES",
}

export enum CommonButtonType {
  TO_CART = "TO_CART",
  BUY_IN_ONE_CLICK = "BUY_IN_ONE_CLICK",
}
